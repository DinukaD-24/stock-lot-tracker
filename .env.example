# Backend API port (used by docker-compose.yml)
API_PORT=8000

# Frontend port (used by docker-compose.yml)
FRONTEND_PORT=3000

# Frontend build-time API URL — must match API_PORT above when running via Docker
NEXT_PUBLIC_API_URL=http://localhost:8000